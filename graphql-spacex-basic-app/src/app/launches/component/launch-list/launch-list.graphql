query launchesList($limit: Int!) {

    launchesPast(limit: $limit) {
        id,
        mission_name,
        launch_date_local,
        links {
            mission_patch_small,
            reddit_media,
            flickr_images
        },
        rocket {
            rocket_name,
            rocket_type
        }
    }
    
}